# shared_core

Código Dart compartilhado entre `admin_web` e `student_web`: cliente de API (`ApiClient`), estado de autenticação (`authSessionProvider`), enum `Role` e tema Material 3 (`AppTheme`).

Pacote interno do monorepo SaaSGym — não publicado no pub.dev (`publish_to: none`).
