# Design System e padrão de CRUD (frontend)

Escrito na Sprint de Consolidação entre a Sprint 2 (módulo de Alunos) e a Sprint 3 (Professores) do frontend `admin_web`. Documenta o que ficou implícito no código durante as Sprints 1-2: a identidade visual e o padrão arquitetural que **todo módulo CRUD futuro** (Professores, Turmas, Planos, Matrículas, Avaliações, Financeiro) deve seguir, reaproveitando a infraestrutura em vez de recriá-la.

## Onde vive

Todo o Design System mora em `packages/shared_core/lib/src/design_system/` — nunca em `admin_web` ou `student_web` diretamente, porque os dois frontends compartilham a mesma identidade visual. Ponto único de import: `package:shared_core/shared_core.dart` (o barrel `design_system.dart` é reexportado por ele). Nunca importar um arquivo de token/widget do Design System por caminho direto.

```
design_system/
  tokens/      app_colors, app_typography, app_spacing, app_radius,
               app_shadows, app_motion, app_breakpoints, app_icons
  theme/       app_theme (AppTheme.darkPremium()), design_system_context
               (context.colors, context.isMobile/isTablet/isDesktop)
  widgets/
    buttons/   surfaces/   feedback/   data/   inputs/   layout/
    navigation/  overlays/
```

## Princípios (não renegociar sem decisão explícita)

- **Cor por papel, nunca por valor.** `AppColorScheme` é um `ThemeExtension`, não `static const` — isso é o que permite, no futuro, tema por academia (multi-tenant) sem reescrever nenhuma tela. Nunca `Color(0xFF...)` inline numa tela de feature.
- **Ícones exclusivamente `lucide_icons_flutter`**, mapeados por nome semântico em `AppIcons` (ex.: `AppIcons.edit`, não `LucideIcons.pencil` direto na tela). Nunca `Icons.*` (Material) numa tela nova.
- **Dark mode único** — não existe light theme nesta fase do produto.
- **Componente nasce de necessidade real.** Nenhum componente é criado "porque vai precisar" — só quando uma tela real precisa dele. Isso manteve o catálogo enxuto nas Sprints 1-2 (ver histórico abaixo).
- **Todo componente novo entra na Component Gallery** (`/design-system`, `admin_web/lib/features/design_system_gallery/`) antes de ser considerado pronto — variações, estados, e uma descrição curta do propósito. Isso é parte da definição de "pronto", no mesmo nível de "compila" e "testes verdes". A Gallery é a documentação viva do Design System — este arquivo documenta a *arquitetura*, a Gallery documenta o *catálogo visual*.

## Princípio de auditabilidade financeira (Módulo 3 em diante)

**Nenhuma entidade financeira pode sofrer alterações por efeitos colaterais invisíveis. Toda operação financeira deve partir de uma ação explícita do usuário ou de um processo auditável e produzir registro de auditoria.**

Aplica-se a `Mensalidade`/`Lancamento` e a qualquer entidade financeira futura (ex.: um eventual módulo de assinatura/cobrança recorrente). Na prática:

- Nenhum método de serviço muda `valor`/`status`/`desconto`/`multa`/`dataPagamento` de uma `Mensalidade` ou cria/edita um `Lancamento` como efeito colateral de outra operação sem que isso seja auditado — o único caso hoje em que uma operação financeira nasce "de tabela" (o `Lancamento` criado por `MensalidadesService.marcarPaga`) é ele próprio o produto de uma ação explícita do usuário (marcar como paga) e está coberto por `MENSALIDADE_PAGA` no `AuditLog` (a criação do `Lancamento` em si não precisa de uma entrada de auditoria própria porque é parte da mesma transação/decisão, já registrada).
- Toda transição de estado financeiro (`gerar`, `marcarPaga`, `cancelar`, `update`, `remove` de `Mensalidade`; `create`/`update`/`remove` de `Lancamento`) já grava `auditService.record(...)` — nenhum novo endpoint financeiro deve ser criado sem essa chamada, mesmo padrão já seguido desde o MS1 (`docs/17-modulo-3-financeiro-analise.md`).
- Nenhum scheduler/job em background deve alterar dado financeiro silenciosamente — é por isso que a geração de `Mensalidade` é sob demanda (`docs/17`, item 2), não automática: até existir infraestrutura de cron auditável, "processo auditável" nesta frase significa uma ação humana explícita.
- Vale também para o frontend: nenhuma tela deve disparar uma mutação financeira (`marcarPaga`, `cancelar`, `gerar`) sem confirmação explícita do usuário (mesmo padrão de `AppConfirmDialog`/diálogo próprio já usado em Matrículas) — nunca uma ação automática ao carregar uma tela ou como resultado indireto de outra ação.

## Catálogo de componentes (por categoria da Gallery)

| Categoria | Componentes | Propósito |
|---|---|---|
| Buttons | `AppButton` | Único botão do produto — 6 variantes, loading/disabled, ícone opcional |
| Cards | `AppCard`, `MetricCard` | Superfície de conteúdo genérica; indicador/KPI (Dashboard hoje, Financeiro/Relatórios depois) |
| Lists | `AppListToolbar`, `AppListTile`, `AppDetailRow` | Barra de lista (busca+filtros+ação primária), item de lista, par rótulo/valor somente leitura (painéis de detalhe) |
| Chips | `AppBadge` | Status e contadores curtos — tom semântico nunca substitui a cor de marca |
| Empty States | `EmptyState`, `EmptyState.comingSoon` | Vazio acionável; placeholder de funcionalidade futura com a tag da sprint responsável |
| Skeleton | `LoadingSkeleton` | Placeholder de carregamento com shimmer, respeita `MediaQuery.disableAnimations` |
| Inputs | `AppTextField`, `AppSelect`, `AppDateField`, `AppAvatarPicker` | Campos de formulário — mesmo chassi de validação (`_AppFieldChrome`, privado); nenhum sabe de regra de domínio |
| Forms | `AppFormRow`, `AppFormErrorBanner` | Grid responsivo de campos (2 colunas desktop / 1 mobile); erro de formulário (nunca `SnackBar`) |
| Dialogs | `AppConfirmDialog` | Único diálogo de decisão do produto — 5 variantes de tom (`confirm`/`danger`/`warning`/`success`/`error`) |
| Navigation | `AppBreadcrumb`, `AppHeader`, `AppSidebar`, `AppPagination` | Navegação do shell; paginação server-side (`page`/`total`/`pageSize`, nunca lista client-side) |
| Motion | `AppMotion` | Durações (`fast`=120ms, `base`=180ms, `slow`=280ms) e curva padrão de toda transição |

## O padrão de CRUD — convenção oficial do projeto

Definido na Sprint 2 (Alunos) e **validado como convenção oficial ao final do Módulo 1** (2026-07-11): a mesma estrutura (Lista → Formulário → Detalhe, descrita abaixo) foi reaplicada com sucesso em três entidades independentes — Alunos (Sprint 2), Professores (Sprint 3) e Planos (Módulo 1) — sem exigir **nenhum** componente novo do Design System além do que Alunos já havia estabelecido. Isso não é mais "o jeito que Alunos fez as coisas": é o contrato arquitetural que **todo módulo CRUD do produto** (presente e futuro — Matrículas, Financeiro, Agenda, Avaliações Físicas, Frequência, Treinos) deve seguir por padrão. Divergir dele exige uma decisão explícita e justificada, documentada aqui — não é uma opção default.

Nenhum módulo novo deve reinventar layout — deve reutilizar esta infraestrutura.

### Lista

`AppListToolbar` (busca com debounce de 400ms + filtro de status + ação primária "Novo X") → itens via `AppListTile` → `AppPagination`. 5 estados obrigatórios: carregando (skeleton com a mesma forma dos itens reais), erro, vazio sem filtro, vazio com filtro ativo (mensagem diferente), populado. Nunca tela quebrada.

Referência: `admin_web/lib/features/alunos/alunos_screen.dart`.

### Formulário

Seções em `AppCard` — no mínimo: Dados pessoais/principais, Contato (se aplicável), Endereço (se aplicável), Observações, Ações. Campos em `AppFormRow` (2 colunas desktop, empilha no mobile). Validação de campo **sempre** o chassi do MS1 (borda vermelha + mensagem abaixo do campo + ícone — nunca `SnackBar` para erro de campo). Erro de salvar sempre em `AppFormErrorBanner`. Botão salvar com 4 estados visíveis: Salvar / Salvando.../ Sucesso / Erro.

Referência: `admin_web/lib/features/alunos/aluno_form_screen.dart`.

### Detalhe (painel)

Cabeçalho: avatar somente-leitura (`AppAvatarPicker(enabled: false)`), nome, `AppBadge` de status, ações (Editar/Ativar-Inativar/Remover) via `AppButton`. Confirmação de remoção sempre via `AppConfirmDialog.show(variant: danger)` — nunca `AlertDialog` cru. Seções reais usam `AppDetailRow` dentro de `AppFormRow` (o grid de leitura rima visualmente com o grid do formulário de edição da mesma entidade). Seções de funcionalidade ainda não implementada usam `EmptyState.comingSoon` com a tag da sprint responsável do roadmap (`docs/08-roadmap.md`) — nunca dado inventado; se a sprint responsável ainda não tem número definido, usar `"EM BREVE"` em vez de chutar um número.

Cabeçalho responsivo via `context.isMobile` (empilha em coluna no mobile) — **nunca** um `Row`/`Wrap` sem `Expanded`/`Flexible` ao redor de texto de tamanho variável (ver "Bug recorrente" abaixo).

Referência: `admin_web/lib/features/alunos/aluno_detail_screen.dart`.

### Roteamento

- Telas que vivem dentro do `ShellRoute` (Dashboard, listas, Perfil) **não** têm `Scaffold` próprio — herdam o do `AppShell`.
- Telas de rota cheia fora do `ShellRoute` (`/alunos/novo`, `/alunos/:id`, `/alunos/:id/editar`) **precisam** do próprio `Scaffold` — não há nenhum ancestral Material para elas. Confundir os dois casos não é pego pelo `flutter analyze` nem por smoke test isolado (que sempre embrulha tudo num `Scaffold` de teste) — só aparece testando a navegação real. Foi um bug real da Sprint 2 (MS4), corrigido e coberto por teste de integração desde então.

## Bug recorrente (documentar para não repetir)

`Row`/`Wrap` com `mainAxisSize.min` sem `Expanded`/`Flexible` ao redor de um texto de tamanho variável estoura (`RenderFlex overflowed`) assim que usado num contexto mais estreito do que o primeiro teste cobriu. Já aconteceu em `AppButton`, `AppBreadcrumb`, `AppHeader` (mobile), `MetricCard` (dentro de `Wrap`) e no cabeçalho do `AlunoDetailScreen` (mobile). Ao criar qualquer widget novo — do Design System ou de tela —, sempre testar com texto longo **e** num container estreito antes de considerar pronto.

## Débito técnico conhecido (levantado nesta Sprint de Consolidação)

### 1. `ProfessoresScreen`/`ProfessorFormScreen`/`ProfessorDetailScreen` são pré-Design-System

As três telas de Professores (`admin_web/lib/features/professores/`) foram implementadas antes da Sprint 1 (Design System) e da Sprint 2 (padrão de CRUD) — usam `Scaffold`/`TextField`/`ListTile`/`AlertDialog` crus do Material padrão, sem nenhum componente do Design System. **O backend, o cliente HTTP (`ProfessoresApi`) e o roteamento (`app_router.dart`) já estão 100% prontos e espelham exatamente Alunos** — a Sprint 3 é puramente uma reescrita de UI reaproveitando a infraestrutura, não um CRUD novo do zero. Nenhuma mudança de modelo, API ou rota é esperada.

### 2. `AlunosApi`/`ProfessoresApi`/`PlanosApi` eram estruturalmente idênticas — resolvido com `CrudApi<T>`

**Status: implementado** (Módulo 1, MS2). A 3ª repetição idêntica (Planos) confirmou o gatilho combinado — extraída `packages/shared_core/lib/src/common/crud_api.dart`:

```dart
abstract class CrudApi<T> {
  CrudApi(this.dio, {required this.resourcePath, required this.fromJson});
  final Dio dio;
  final String resourcePath;
  final T Function(Map<String, dynamic>) fromJson;

  Future<PaginatedResult<T>> list({String? search, UserStatus? status, int page = 1, int pageSize = 20}) async { ... }
  Future<T> get(String id) async { ... }
  Future<T> create(Map<String, dynamic> dados) async { ... }
  Future<T> update(String id, Map<String, dynamic> dados) async { ... }
  Future<T> updateStatus(String id, UserStatus status, {String? motivo}) async { ... }
  Future<void> remove(String id) async { ... }
}

class AlunosApi extends CrudApi<Aluno> {
  AlunosApi(super.dio) : super(resourcePath: '/alunos', fromJson: Aluno.fromJson);
  Future<Aluno> uploadFoto(String id, {required Uint8List bytes, required String filename}) async { ... }
}

class PlanosApi extends CrudApi<Plano> {
  PlanosApi(super.dio) : super(resourcePath: '/planos', fromJson: Plano.fromJson);
}
```

**`uploadFoto` deliberadamente fora da base** — não é universal (Plano não tem foto, Matrícula não vai ter); cada subclasse que precisa declara o próprio método usando `dio`/`resourcePath` herdados (public, não `_dio` privado — precisam ser acessíveis a subclasses em outro arquivo). Métodos extras de uma única entidade (ex.: `Matricula.renovar`, quando o Módulo 2 chegar) entram do mesmo jeito, como método adicional na subclasse — a base nunca tenta prever tudo.

**`CrudRepository<T>`/`CrudProvider<T>` avaliados e rejeitados** — sem camada de "Repository" em nenhum lugar do projeto hoje (os `*Api` já são a camada de acesso a dado), e `CrudProvider<T>` esconderia a declaração de 1 linha (`final xApiProvider = Provider<XApi>((ref) => XApi(...))`) atrás de uma função genérica sem reduzir duplicação real, só escondendo. Ambos ficam de fora até haver necessidade comprovada — mesmo princípio já aplicado a componente de UI, agora aplicado a camada de dados.

**Princípio permanente: filtros e operações específicas de domínio nunca entram em `CrudApi<T>`.** `renovar` (Matrícula), `listarVencidas` (Matrícula), `listarPorPlano` (Matrícula/Aluno) e qualquer método parecido são declarados na subclasse concreta, do mesmo jeito que `uploadFoto` já é — nunca um parâmetro opcional a mais em `list()` nem um novo método genérico na base. `CrudApi<T>` só cresce se um **novo** método se repetir de forma idêntica em 3 ou mais subclasses (mesmo gatilho de "3ª repetição confirmada" que já validou a criação da própria classe) — até lá, a base fica pequena e estável, e cada entidade é livre para ter a forma que o próprio domínio pedir.

### 3. Component Gallery tinha lacunas (corrigidas nesta Sprint)

`MetricCard` estava exportado e em uso real (Dashboard) mas não aparecia na Gallery — violava a própria regra do projeto. `AppAvatarPicker` e `AppPagination` só mostravam um estado cada. Todas as seções da Gallery ganharam uma descrição de uma linha (rule nova: "todo componente deve ter uma pequena descrição e demonstrar seus estados"). Corrigido em `admin_web/lib/features/design_system_gallery/design_system_gallery_screen.dart`.

### 4. `AppPagination` tinha overflow em mobile — nunca pego antes (Módulo 1, MS3)

Mesmo padrão de bug de sempre (`Text` de largura variável sem `Flexible`/ellipsis dentro de um `Row`), só que **dentro do próprio Design System**, existindo desde a Sprint 2 sem nunca ter sido testado numa largura estreita de verdade. Corrigido com `Flexible`+ellipsis, teste de regressão adicionado ao smoke test. Lição registrada na memória do projeto: um componente "aprovado" há sprints não está imune a este padrão — vale reconferir sempre que ele aparecer numa tela nova, não só quando é criado.

### 5. Rotas `/:id/editar` sem `key: ValueKey(id)` — achado real no MS4 de Matrículas

Nenhuma rota do `app_router.dart` (`/alunos/:id/editar`, `/professores/:id/editar`, `/planos/:id/editar`, `/matriculas/:id/editar`) passa um `key:` pro widget do formulário. Sem isso, navegar direto de um "`/editar`" pra outro com id diferente **sem desmontar a tela no meio** faz o Flutter reaproveitar o mesmo `State` (`didUpdateWidget`, não `initState` de novo) — a tela continua mostrando os dados da entidade anterior, embora o payload salvo use o novo id da rota (dado exibido e dado salvo divergem). Não se manifesta no fluxo normal via UI (List → push Detalhe → push Editar → pop → pop; cada `push` cria uma `Page`/`Route` nova, com `Element` próprio) — só apareceu testando navegação direta por URL. Corrigido **só em Matrículas** (`key: ValueKey('matricula-\${id}')` em `/matriculas/:id/editar` e `/matriculas/novo`); Alunos/Professores/Planos ficam com a mesma lacuna, não corrigida agora por ser sistêmica e fora do escopo do Módulo 2 — considerar ao tocar nessas rotas de novo.

## Valor monetário — primeira ocorrência, sem componente novo ainda

`Plano.valor` (Módulo 1, MS4) é o primeiro campo monetário do produto. Implementado com `AppTextField` simples (`keyboardType: numberWithOptions(decimal: true)`, aceita vírgula ou ponto, validado e convertido pra `double` só no payload) — **sem** um componente `AppCurrencyField` novo, porque é a 1ª ocorrência, não a 3ª. Financeiro (Módulo 3) é o candidato natural a confirmar a repetição — se `Mensalidade`/`Lancamento` precisarem do mesmo tratamento, aí sim vale extrair um componente, com máscara de entrada e formatação consistente. Mesmo gatilho já usado para `CrudApi<T>`, agora aplicado a componente de input.

**Regra permanente de normalização**: todo valor monetário é normalizado no frontend **antes** de sair pro backend — `_valorController.text.trim().replaceAll(',', '.')` convertido pra `double` no `_construirPayload()`, nunca a string crua do campo. O banco continua usando `Decimal` (`@db.Decimal(10, 2)`, nunca `Float`) como representação oficial em todo model financeiro — `Plano.valor` hoje, `Mensalidade.valor`/`desconto`/`multa` e `Lancamento.valor` quando o Módulo 3 chegar. `PlanosService.toResponse()` já converte `Decimal → number` antes de qualquer resposta HTTP (nunca vaza um `Decimal.js` cru pro cliente) — mesmo contrato vale pra todo campo monetário futuro.

## Seleção de entidade — primeira ocorrência, sem componente novo ainda

`MatriculaFormScreen` (Módulo 2, MS4) é o primeiro formulário do produto que referencia **outra entidade** (Aluno, Plano) em vez de um enum fixo (`Periodicidade`, `UserStatus`, `Sexo`). Implementado com `AppSelect` simples — busca a lista de Alunos/Planos ATIVOS uma vez (`pageSize: 100`, o máximo aceito pelo backend) ao abrir o formulário e popula `AppSelectOption`s com id/nome — **sem** um componente `AppAutocompleteField` novo, mesmo gatilho já usado pra `AppCurrencyField`: é a 1ª ocorrência, não a 3ª, e o volume de alunos/planos de uma academia nesta fase do produto cabe numa lista simples sem busca por digitação.

Carregamento desacoplado do widget de propósito — `_carregarOpcoes()`/`_carregarMatricula()` são métodos próprios em `MatriculaFormScreen`, não lógica inline no `build()`. Se o volume de registros um dia comprometer a usabilidade de um dropdown simples (ou aparecer um 2º consumidor real do mesmo padrão — Financeiro → Aluno, Agenda → Aluno/Professor), a troca por um `AppAutocompleteField` fica isolada na camada de UI, sem tocar a regra de negócio. **Reavaliar `AppAutocompleteField` só quando houver múltiplos consumidores reais ou evidência de que o volume de registros compromete a usabilidade** — não antes.

Em modo edição, `Aluno`/`Plano`/`Data de início` aparecem como `AppSelect`/`AppDateField` **desabilitados** (não somem) — `UpdateMatriculaDto` só aceita `valor`/`diaVencimento` (planoId/alunoId/dataInicio são imutáveis, docs/16 item 12); mostrar desabilitado deixa claro que a informação existe mas não é editável por este formulário, em vez de escondê-la.

## Diálogo com campos — quando `AppConfirmDialog` não basta

`AppConfirmDialog` só tem título/descrição estáticos — não serve quando a confirmação precisa capturar um dado (ex.: `Cancelar` matrícula exige um `MotivoCancelamento` categorizado obrigatório + detalhe condicional, Módulo 2 MS5). Nesse caso, o padrão é um `StatefulWidget` privado da própria tela de feature (não um componente novo do Design System, não exportado, sem uso fora dali) que reproduz o mesmo chassi visual do `AppConfirmDialog` (`Dialog` + `colors.card` + `AppRadius.lg` + `BorderSide(color: colors.border)` + `EdgeInsets.all(AppSpacing.xl)`) e compõe o conteúdo com os átomos já existentes (`AppSelect`/`AppTextField`/`AppFormErrorBanner`/`AppButton`). Referência: `_CancelarMatriculaDialog` em `admin_web/lib/features/matriculas/matricula_detail_screen.dart`. Só vale extrair isso pra um componente genérico (`AppPromptDialog<T>` ou parecido) se um **segundo** fluxo precisar do mesmo formato — mesmo gatilho de "3ª repetição"/"2º consumidor real" já usado em outras decisões deste documento.

**Atualização (Módulo 3 MS3)**: `_AcoesMensalidadeDialog` (`admin_web/lib/features/financeiro/mensalidades_screen.dart`) é o 2º consumidor real deste padrão — mesmo chassi, agora com múltiplas views internas (menu → pagar/editar/cancelar) em vez de um único formulário. Ainda não extraído: os dois casos divergem o suficiente (motivo único vs. menu com sub-formulários) que uma abstração agora seria prematura — mas com 2 consumidores reais, uma 3ª ocorrência deve disparar a avaliação de extração (`AppPromptDialog<T>`), não apenas ser adicionada como mais um `StatefulWidget` privado.

**Atualização (Módulo 3 MS4)**: `_AcoesLancamentoDialog` (`admin_web/lib/features/financeiro/caixa_screen.dart`) é o 3º consumidor real — o gatilho que a atualização do MS3 previu. Ainda **não extraído nesta sprint** (MS4 tinha escopo próprio definido, sem abertura pra reabstrair o Design System no meio da implementação) — fica registrado aqui como candidato prioritário pra próxima Sprint de Consolidação: os 3 casos (motivo único, menu com sub-formulários, menu com sub-formulários + navegação condicional) têm chassi idêntico e visões internas como a única variação real, o que sugere que `AppPromptDialog<T>` (ou um `_AcoesDialogShell` de views) já compensaria o investimento.

**Veredito final (Sprint de Consolidação, 2026-07-13)**: o Módulo 4 elevou a contagem real pra **7 consumidores** (`_CancelarMatriculaDialog`, `_AcoesMensalidadeDialog`, `_AcoesLancamentoDialog`, `_AcoesModalidadeDialog`, `_AcoesRecorrenciaDialog`, `_AcoesTurmaAlunoDialog`, `_AulaAcoesDialog`) — muito além do gatilho de "3ª ocorrência" já superado no Módulo 3. Mesmo assim, a extração de um `AppPromptDialog<T>`/`_AcoesDialogShell` genérico foi **rejeitada** nesta sprint depois de comparar os 7 casos linha a linha: o número de views internas varia de 1 (`_AcoesTurmaAlunoDialog`) a 4 (`_AulaAcoesDialog`, `_AcoesMensalidadeDialog`), os campos e o grafo de transição entre views são específicos de cada entidade, e alguns consumidores nem seguem o formato "menu → view" (`_CancelarMatriculaDialog` é um formulário único que devolve um valor, não chama a API sozinho). Forçar uma abstração genérica exigiria um builder-por-view como escape hatch — nesse ponto é indireção pura, sem simplificação real, contrariando o próprio princípio de "não abstrair por hipótese" deste documento. **Duas extrações menores, mecânicas e de risco zero foram feitas** (não a máquina de views): (1) `mensagemErroApi` — a função `_mensagemErro(DioException)` idêntica em 15 arquivos foi extraída para `shared_core` (`src/api/dio_error_message.dart`); (2) o wrapper visual do diálogo (`Dialog` + `colors.card` + `AppRadius.lg` + `BorderSide` + `EdgeInsets.all(AppSpacing.xl)`, ~10 linhas idênticas por diálogo, puro markup sem estado) é um candidato igualmente válido (`AppDialogChrome`) mas **não foi extraído nesta sprint** — fica documentado como próxima oportunidade de baixíssimo risco, não como débito pendente de decisão. O padrão "diálogo próprio por feature, sem componente genérico de views" está encerrado como a convenção definitiva do produto.

## Convenção de testes

- `flutter analyze` limpo nos 3 pacotes (`shared_core`, `admin_web`, `student_web`) antes de qualquer entrega.
- Testes de widget: smoke test por componente em `shared_core` (`design_system_smoke_test.dart`) + teste de integração de fluxo real em `admin_web` (`widget_test.dart`, navegação via `AdminApp` real, nunca componente isolado apenas).
- Scripts de screenshot são **descartáveis** — vivem em `admin_web/test/_<nome>_screenshot_test.dart`, nunca commitados, sempre apagados após a revisão visual. Fontes reais (Archivo/JetBrains Mono/Lucide) carregadas manualmente via `FontLoader` porque `flutter test` usa uma fonte de fallback (glifos em bloco) por padrão — isso é esperado só no ambiente de teste, não indica bug de produto.
- **Gotcha de `tester.runAsync`**: capturar PNG via `RenderRepaintBoundary.toImage()` num script de screenshot exige `await tester.runAsync(() async { ...toImage()... })` — sem isso, o `toImage()` é trabalho assíncrono real do engine fora do fake clock do `flutter test`; a 1ª captura funciona, mas o runner trava (sem erro, sem timeout) ao tentar avançar pro teste seguinte.

## Histórico

- **Sprint 1** (2026-07-10): fundação do Design System — tokens, tema, `AppButton`/`AppCard`/`AppBadge`/`EmptyState`/`LoadingSkeleton`/`MetricCard`/`AppListTile`, shell de navegação (`AppSidebar`/`AppHeader`/`AppBreadcrumb`), Dashboard operacional.
- **Sprint 2** (2026-07-11): módulo de Alunos como referência de CRUD — `AppTextField`/`AppSelect`/`AppDateField`, `AppAvatarPicker`, `AppConfirmDialog`, `AppPagination`, `AppListToolbar`, `AppFormRow`, `AppFormErrorBanner`, `AppDetailRow`. Lista, formulário e painel de detalhe consolidados como padrão oficial.
- **Sprint de Consolidação** (2026-07-11): este documento; correção das lacunas da Component Gallery (`MetricCard`, estados de `AppAvatarPicker`/`AppPagination`); levantamento do débito técnico de Professores e da duplicação `AlunosApi`/`ProfessoresApi`. Nenhuma funcionalidade nova.
- **Sprint 3** (2026-07-11): módulo de Professores reescrito com o padrão de Alunos — 100% reuso do Design System, zero componente novo, backend/API/rotas já existiam intactos.
- **Fase 2 — Módulo 1 (Planos), MS2** (2026-07-11): `CrudApi<T>` extraído e adotado por `AlunosApi`/`ProfessoresApi`/`PlanosApi` (3ª repetição confirmada, gatilho combinado atingido). Ver item 2 da seção "Débito técnico" acima.
- **Fase 2 — Módulo 1 (Planos), MS3** (2026-07-11): `PlanosScreen` (lista), zero componente novo. `AppPagination` corrigida (item 4 da seção "Débito técnico").
- **Fase 2 — Módulo 1 (Planos), MS4** (2026-07-11): `PlanoFormScreen`, zero componente novo. Primeira entrada monetária do produto (`valor`), sem `AppCurrencyField` — ver seção "Valor monetário" acima.
- **Fase 2 — Módulo 1 (Planos), MS5** (2026-07-11): `PlanoDetailScreen`, encerrando o Módulo 1 — zero componente novo, mesmo padrão de `AlunoDetailScreen`/`ProfessorDetailScreen` (`AppDetailRow`+`AppFormRow`, `PopScope` propagando `alterado`, `AppConfirmDialog` na remoção). Seções "Alunos matriculados" e "Financeiro" como `EmptyState.comingSoon` com a nova tag `'MÓDULO N'` (substitui `'SPRINT N'` a partir desta fase). Bug real encontrado só no screenshot mobile — `Expanded` aplicado direto na variável `info` reusada num `Column` de altura irrestrita (dentro do `SingleChildScrollView`); corrigido deixando `info` "plana" e envolvendo com `Expanded` só no ponto de uso do `Row` desktop, mesmo padrão de `AlunoDetailScreen`. Com Alunos, Professores e Planos completos, a base de CRUD está consolidada para iniciar Matrículas e Financeiro.
- **Módulo 2 (Matrículas), MS4** (2026-07-12): `MatriculaFormScreen` — zero componente novo do Design System. Primeira seleção de entidade do produto (Aluno/Plano via `AppSelect`, sem `AppAutocompleteField` — ver seção "Seleção de entidade" acima). Achado real: rotas `/:id/editar` sem `key: ValueKey(id)` reaproveitam `State` entre ids diferentes ao navegar direto (item 5 da seção "Débito técnico"); corrigido só nas rotas de Matrículas.
- **Módulo 2 (Matrículas), MS5** (2026-07-12): `MatriculaDetailScreen`, encerrando o Módulo 2 — zero componente novo, mesmo padrão de `AlunoDetailScreen`/`PlanoDetailScreen`. Matriz de estados e transições documentada em `docs/16` antes da implementação. Primeiro diálogo de confirmação com campo do produto (`Cancelar` — motivo categorizado) — ver seção "Diálogo com campos" acima. `key: ValueKey(id)` também aplicado à rota `/matriculas/:id` (Detalhe), mesmo achado do MS4.
- **Módulo 3 (Financeiro), MS2** (2026-07-12): camada de dados (`Mensalidade`/`Lancamento` em `shared_core`, `MensalidadesApi`/`LancamentosApi`, providers) — zero componente novo. Princípio de auditabilidade financeira registrado nesta sprint (seção "Princípio de auditabilidade financeira" acima). `MensalidadesApi` não reaproveita `create()` de `CrudApi<T>` — Mensalidade só nasce via `gerar()` (bulk); `get`/`update`/`remove` continuam herdados. `LancamentosApi` reaproveita `create`/`get`/`update`/`remove` inteiros, só `list()` vira `listLancamentos` próprio (mesmo motivo de tipo incompatível já documentado em `MatriculasApi`/`MensalidadesApi`).
- **Módulo 3 (Financeiro), MS3** (2026-07-13): `MensalidadesScreen` — primeira tela do produto organizada por competência (mês/ano) em vez de "todos os registros por padrão"; busca/status continuam no `AppListToolbar` de sempre, como complemento. `atrasada` (computado pelo backend) virou tom de badge, não um filtro novo. Zero componente novo — `_AcoesMensalidadeDialog` reusa o mesmo padrão de diálogo-próprio-com-chassi-de-`AppConfirmDialog` já registrado abaixo para `_CancelarMatriculaDialog` (Matrículas MS5), agora com um segundo consumidor real. `search` adicionado a `ListMensalidadesQueryDto` (mesmo ajuste já feito em Matrículas MS3). Achado de validação (não é bug, é comportamento do Flutter Web/CanvasKit): o texto de título/descrição de `AppConfirmDialog` e o label flutuante de `AppSelect` não são expostos na árvore de acessibilidade — só os botões (`AppButton`) e o `aria-label` do `<input>` real de `AppTextField` são. Relevante para quem for escrever testes ou scripts de automação contra estas telas.
- **Módulo 3 (Financeiro), MS4** (2026-07-13): `CaixaScreen` — análise de domínio prévia concluiu Livro Caixa, não cadastro genérico (docs/17). Zero componente novo — `MetricCard` (já previsto desde o Dashboard pra "Financeiro/Relatórios futuros") cobre o resumo financeiro (Receitas/Despesas/Saldo); `_AcoesLancamentoDialog` é o 3º consumidor real do padrão "diálogo próprio com chassi de `AppConfirmDialog`" (`_CancelarMatriculaDialog` → `_AcoesMensalidadeDialog` → `_AcoesLancamentoDialog`) — ainda não extraído (ver seção "Diálogo com campos" acima), mas com 3 consumidores reais este é o ponto de reavaliar a extração numa próxima Sprint de Consolidação. Novo campo `origem` (`LancamentoOrigem`) em `Lancamento` substitui heurística por `mensalidadeId != null`. `MensalidadesScreen` ganhou parâmetros iniciais opcionais (mês/ano/busca via query string) pra suportar navegação cruzada a partir de uma linha do Caixa gerada por Mensalidade. Sidebar "Financeiro" dividida em "Mensalidades" e "Caixa" (convenção de nomear pela entidade, não por termo guarda-chuva). Achado de validação: `AppSelect`, ao reabrir, alinha o item **atualmente selecionado** com a posição do campo — não sempre o item 0 no topo — então a coordenada de clique de um item muda dependendo do valor selecionado no momento; relevante pra automação de testes.
- **Módulo 3 (Financeiro), MS5** (2026-07-13): `PainelFinanceiroScreen`, encerrando o Módulo 3 — análise de domínio prévia concluiu Painel Financeiro Gerencial, não mais um livro-caixa (docs/17). Zero componente novo — `MetricCard` cobre os 5 KPIs do topo (Receita Prevista/Recebida/Inadimplência/Despesas/Saldo); a "Evolução mensal" é uma tabela composta com `AppCard`+`Row`+`Divider` no desktop e cards empilhados no mobile (`context.isMobile`), sem gráfico nem biblioteca de chart — decisão explícita, ver docs/17. Correção ao achado do MS4 sobre `AppSelect`: o alinhamento "item selecionado na posição do campo" só vale quando a lista inteira cabe na tela sem sair dos limites — com uma lista de 12 itens (Mês) e o campo perto do topo, o Flutter *clampeia* a posição pra manter a lista visível, deslocando-a pra baixo em vez de deixar os primeiros itens com coordenada negativa. Automação de teste precisa ler a posição real de cada abertura via screenshot, não assumir uma fórmula fixa.
- **Módulo 4 (Agenda), MS2** (2026-07-13): camada de dados — `Modalidade`/`Feriado` em `shared_core`, zero componente novo (sem tela ainda). `ModalidadesApi`/`FeriadosApi` são CRUD puro sobre `CrudApi<T>` — mesmo caso de `PlanosApi`, nenhum método próprio necessário. `FeriadosApi` documenta `updateStatus`/`status` como membros herdados sem uso (Feriado não tem campo de status), mesmo padrão já registrado em `MensalidadesApi`/`LancamentosApi`. Nenhuma API de `Turma`/`Recorrencia`/`Aula` antecipada — só o que o MS1 (backend) já cobre.
- **Módulo 4 (Agenda), MS3** (2026-07-13): `TurmasScreen`/`TurmaFormScreen`/`TurmaDetailScreen` — zero componente novo, espelham `PlanosScreen`/`PlanoFormScreen`/`PlanoDetailScreen` ponto a ponto. Única divergência deliberada do padrão de seleção de entidade (seção "Seleção de entidade" acima): `modalidadeId`/`professorId` continuam `AppSelect` editáveis em modo edição, ao contrário de `alunoId`/`planoId` em `MatriculaFormScreen` (que viram campo desabilitado) — porque o backend aceita a troca sem quebrar invariante nenhuma, diferença de regra de negócio, não de padrão de UI. `ModalidadesScreen` é o 4º consumidor real do padrão "diálogo próprio com chassi de `AppConfirmDialog`" (`_CancelarMatriculaDialog` → `_AcoesMensalidadeDialog` → `_AcoesLancamentoDialog` → `_AcoesModalidadeDialog`), escolhido em vez de rotas cheias por ter só 2 campos (nome, cor) — mesmo critério de proporcionalidade já usado para decidir o oposto (Turma, 6 campos, ganhou rotas). Dois tokens novos em `AppIcons` (`turmas`, `modalidade`); nenhum outro componente novo.
- **Módulo 4 (Agenda), MS4** (2026-07-13): Recorrência gerenciada inteiramente dentro de `TurmaDetailScreen` (`_RecorrenciasSection`/`_RecorrenciaFormDialog`/`_AcoesRecorrenciaDialog`) — nem tela nem rota própria, 5º consumidor do padrão de diálogo, e o 1º caso onde a entidade só faz sentido no contexto de uma tela-pai já aberta (nunca navegada isoladamente), diferente dos 4 anteriores que eram listas próprias com diálogo de criação. Campo condicional ao valor de outro campo dentro do mesmo formulário (`diaSemana`/`diaDoMes`/`intervaloDias` conforme `tipo`) é a 1ª ocorrência desse padrão de UI no produto — resolvido com `setState` simples mostrando/escondendo `AppTextField`/`AppSelect`, sem componente novo. `RecorrenciasApi` não estende `CrudApi<T>` (ver docs/18, Histórico MS4, e `project_saasgym_crudapi_updatestatus_debt`) — 2ª entidade a confirmar que a classe genérica não serve quando não há paginação/status no molde esperado. Achado de validação: dropdowns de `AppSelect` (`Tipo`/`Dia da semana`) não são clicáveis por `locator(role/text)` em automação — mesmo achado de Módulo 3 MS3/MS5, agora confirmado também pros itens da lista aberta (não só a posição do campo fechado).
- **Módulo 4 (Agenda), MS5** (2026-07-13): `TurmaAluno` (inscrição de aluno na Turma) — 2ª seção embutida em `TurmaDetailScreen` (`_AlunosMatriculadosSection`/`_NovaInscricaoDialog`/`_AcoesTurmaAlunoDialog`), mesmo raciocínio de "só existe no contexto de uma Turma aberta" já registrado no MS4. Diferente de Recorrência, aqui `AppListTile` é reutilizado ponto a ponto para as linhas — é o 1º caso dentro da Agenda onde o item da lista é uma "pessoa" de verdade (aluno), não um padrão/horário. `TurmaAlunosApi` também não estende `CrudApi<T>` (mesmo critério de `RecorrenciasApi`), mas por um motivo ligeiramente diferente: usa `UserStatus`/rota `/status` dedicada (como Turma/Modalidade), só que aninhada — a base genérica ainda não serve porque não há paginação nem `update()` genérico (nenhum outro campo é editável). Diálogo de criação (`_NovaInscricaoDialog`) tem um único campo (`AppSelect` de Aluno) — o mais simples já visto no produto. Contador "N de M vagas ocupadas" no topo da seção só aparece quando a Turma tem `capacidadeMaxima` definida — mesma convenção de "nulo = ilimitado" de `Plano`/`Turma`, sem exibir contagem sem sentido quando não há limite. Nenhum componente novo.
- **Módulo 4 (Agenda), MS6** (2026-07-13): "Aulas" — 3ª seção embutida em `TurmaDetailScreen` (`_AulasSection`/`_GerarAulasDialog`), mas a 1ª que reutiliza `AppPagination` dentro de uma seção embutida — Recorrência/TurmaAluno cabem inteiras numa tela, Aula não (cresce uma linha por ocorrência gerada, sem limite). `AulasApi` também não estende `CrudApi<T>` (mesmo critério de `RecorrenciasApi`/`TurmaAlunosApi`), mas é a 1ª dessas 3 que usa `PaginatedResult<T>` de verdade. `_GerarAulasDialog` é o 1º diálogo do produto que devolve um resultado tipado (`GerarAulasResultado`, não só `bool`) — `_AulasSection` usa o resultado pra montar a mensagem do `SnackBar` ("N aula(s) geradas — M já existiam"), em vez de só recarregar a lista silenciosamente. Linhas de Aula são só leitura (sem diálogo de ações) — reflexo direto do escopo do MS6 (só geração), não uma limitação técnica; cancelamento/professor substituto (MS7) vão precisar de um diálogo de ações como os das outras seções, quando chegarem. Nenhum componente novo.
- **Módulo 4 (Agenda), MS7** (2026-07-13): `CalendarScreen` — a 1ª tela nova do Módulo 4 desde `TurmasScreen` (MS3); Recorrência/TurmaAluno/Aulas do MS6 foram todas seções embutidas em `TurmaDetailScreen`, mas Calendário precisa da própria tela porque cruza turmas (filtro por professor/status/modalidade não faz sentido preso ao Detalhe de uma Turma). Ainda assim, zero componente novo — dia/semana/mês são composição local (`Row`/`Column`/`GridView` manual via `Row`s aninhados) sobre `AppCard`/`AppButton`/`AppBadge`/`AppSelect`/`AppDateField`/`Divider` já existentes; o alternador Dia/Semana/Mês é 3 `AppButton`s (`primary` no selecionado, `outline` nos demais), não um novo "segmented control". `_AulaAcoesDialog` é o 1º diálogo do produto com 3 views (menu/cancelar/substituto, mais que os 2 já vistos em `_AcoesRecorrenciaDialog`/`_AcoesTurmaAlunoDialog`), e o menu se adapta ao dado (aula cancelada esconde "Cancelar"/"Definir substituto", mostra só "Remover") — 1º caso desse tipo de adaptação condicional num menu de ações da Agenda. "Nova aula extra" é o 1º botão de ação de topo (não por linha) que abre um diálogo de criação pré-preenchido com o contexto da tela (data em foco), em vez de nascer vazio. Achado de validação: `AppTextField` com `maxLines > 1` renderiza como `<textarea>` sem `aria-label` (o `<input>` de linha única herda o `aria-label` do grupo; o `<textarea>` não) — nova categoria de achado de acessibilidade pra automação, distinta da já registrada pra `AppSelect` (MS3-MS6).
- **Módulo 4 (Agenda), MS8** (2026-07-13): Frequência — não ganhou seção nem tela própria, virou a 4ª view do `_AulaAcoesDialog` já existente (`menu → frequencia`), ao lado de `cancelar`/`substituto` do MS7. Botão "Registrar frequência" só aparece no menu quando `_aulaRealizada(aula)` é verdadeiro (`status == agendada && data < hoje`) — 1º caso de visibilidade condicional de uma ação inteira (não só o conteúdo do menu) baseada em um cálculo derivado, não num campo armazenado. Cada aluno na lista de frequência é uma linha com nome + um único `AppSelect<PresencaStatus?>` que salva sozinho no `onChanged` (sem botão de salvar por linha) — 1º uso de "auto-save por linha" no produto, modelo de "planilha de chamada" em vez do padrão até então universal de diálogo com botão Salvar. `AulaAlunosApi` também não estende `CrudApi<T>` (mesmo critério das outras APIs aninhadas da Agenda), e `registrarPresenca` é deliberadamente uma única chamada usada tanto pra marcar quanto pra corrigir uma presença (sem operação separada de "editar"). Nenhum componente novo do Design System.
- **Sprint de Consolidação do Módulo 4** (2026-07-13, ver `docs/19-sprint-consolidacao.md` para o relatório completo): auditoria cruzada de Backend/Shared Core/Frontend/Design System/Performance/Documentação/Débitos, sem nenhuma funcionalidade nova. Correções aplicadas: `mensagemErroApi` (extraído de 15 cópias idênticas de `_mensagemErro` pra `shared_core`), `dataCurtaFormat` (extraído de 6 cópias de `DateFormat('dd/MM/yyyy')` cacheado + 2 usos não-cacheados); `AppFieldFocusTracker` extraído do `_FocusTrackingField` privado do `AppTextField` pra `_app_field_chrome.dart` e reaproveitado por `AppSelect`/`AppDateField`, que antes nunca acendiam a borda de foco (inconsistência real entre os 3 campos, agora fechada); `AppPagination`/`AppHeader` — botões de ícone sem nome acessível (`_StepButton`/`_NotificationsButton`) ganharam `Tooltip`; `CalendarScreen` ganhou "Limpar filtros" (única tela com filtro que não tinha, comparado às outras 7); rotas `/alunos`, `/professores`, `/planos` (:id, :id/editar, novo) ganharam `key: ValueKey(id)` — bug já diagnosticado em comentário no próprio `app_router.dart` desde o Módulo 2, nunca corrigido até agora. Rejeitado (com justificativa registrada): `AppCrudDialog`/`AppPromptDialog<T>` genérico (7 consumidores reais, mas views/campos/grafo de transição variam demais — ver seção "Diálogo com campos" acima); `AppEntitySelect` (o padrão "selecionar entidade" só repete 1 linha de mapeamento `AppSelectOption`, tudo em volta — label, sentinela, obrigatoriedade, timing do fetch — é bespoke por tela); `AppListScreen<T>` genérico (duplicação real e extensa entre telas de lista, mas documentada como replicação intencional do padrão desde o Sprint 3 — candidato de sprint futura, não desta). CrudApi<T> — débito técnico encerrado: `RecorrenciasApi`/`TurmaAlunosApi`/`AulasApi`/`AulaAlunosApi` são as 4 confirmações reais de "recurso aninhado nunca estende CrudApi<T>" (independente de paginação/status); `MatriculasApi` é um caso diferente e já resolvido (entidade top-level com transições de ciclo de vida bespoke, mas ainda estende `CrudApi<T>` pra get/update/remove).
