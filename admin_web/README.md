# admin_web

Painel administrativo do SaaSGym (Flutter Web). Ver [`docs/01-arquitetura.md`](../docs/01-arquitetura.md) na raiz do monorepo.

```bash
flutter pub get
flutter run -d chrome --web-port 5000
```
